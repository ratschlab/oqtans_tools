function LABEL = get_label_set_mTIM()

% LABEL = get_label_set_mTIM()
%
% Returns a struct with fields specifying probe annotations.
%
% written by Georg Zeller, MPI Tuebingen, Germany, 2008-2009

LABEL.ambiguous  = -1;

% W and C denote the forward (Watson) and reverse (Crick) strand 
% of the genome, respectively

LABEL.intergenic =  0;
LABEL.intron_W   =  2;
LABEL.intron_C   = -2;
LABEL.exon_W     =  3;
LABEL.exon_C     = -3;

% eof